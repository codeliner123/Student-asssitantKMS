import { Bell, Search, Sparkles } from 'lucide-react';
import { useAppContext } from '../../context/AppContext';
import { Button } from '../ui/Button';
import { hasGeminiKey } from '../../utils/gemini';
export function TopBar() { const { setChatOpen } = useAppContext(); return <div className="sticky top-0 z-20 border-b border-[var(--border)] bg-[var(--bg-primary)]/80 p-4 backdrop-blur"><div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div className="relative md:w-96"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-[var(--text-muted)]" /><input className="min-h-11 w-full rounded-xl border border-[var(--border)] bg-[var(--bg-card)] pl-10 pr-4 outline-none focus:border-[var(--accent-purple)]" placeholder="Search skills, roles, resources..." /></div><div className="flex items-center gap-3"><button className="relative grid h-11 w-11 place-items-center rounded-xl bg-[var(--bg-card)]"><Bell className="h-5 w-5" /><span className="absolute right-3 top-3 h-2 w-2 rounded-full bg-[var(--accent-orange)]" /></button><Button onClick={() => setChatOpen(true)}><span className="flex items-center gap-2"><Sparkles className="h-4 w-4" />Ask EduPath AI</span></Button></div></div>{!hasGeminiKey() && <div className="mt-3 rounded-xl border border-orange-400/30 bg-orange-500/10 px-4 py-3 text-sm text-orange-100">Add your Gemini API key in .env to enable AI features</div>}</div>; }
