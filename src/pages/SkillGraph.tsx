import { useState } from 'react';
import { PolarAngleAxis, PolarGrid, Radar, RadarChart, ResponsiveContainer, Tooltip } from 'recharts';
import { mockSkills } from '../data/mockData';
import { Badge } from '../components/ui/Badge';
import { Button } from '../components/ui/Button';
import { Card } from '../components/ui/Card';
export function SkillGraph() { const [list, setList] = useState(false); const data = mockSkills.map((s) => ({ skill: s.name, level: s.level, category: s.category })); return <div className="space-y-6"><div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div><h2 className="text-3xl font-extrabold">Skill Graph</h2><p className="mt-2 text-[var(--text-secondary)]">Visualize your current strengths and development gaps.</p></div><Button variant="secondary" onClick={() => setList(!list)}>{list ? 'Show Radar' : 'Show List'}</Button></div><Card className="p-5">{list ? <div className="overflow-x-auto"><table className="w-full text-left"><thead className="text-[var(--text-muted)]"><tr><th className="p-3">Skill</th><th>Category</th><th>Level</th></tr></thead><tbody>{mockSkills.map((skill) => <tr key={skill.name} className="border-t border-[var(--border)]"><td className="p-3 font-semibold">{skill.name}</td><td><Badge tone={skill.category === 'Frontend' ? 'purple' : skill.category === 'Backend' ? 'blue' : 'orange'}>{skill.category}</Badge></td><td className="stat-number">{skill.level}%</td></tr>)}</tbody></table></div> : <ResponsiveContainer width="100%" height={460}><RadarChart data={data}><PolarGrid stroke="#353a5a" /><PolarAngleAxis dataKey="skill" stroke="#9499b8" /><Tooltip /><Radar dataKey="level" stroke="#6c63ff" fill="#6c63ff" fillOpacity={0.35} /></RadarChart></ResponsiveContainer>}</Card></div>; }
